<form method="post" action="{{ path('mode_payment_delete', {'id': mode_payment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mode_payment.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
