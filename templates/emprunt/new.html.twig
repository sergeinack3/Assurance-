{% extends 'base.html.twig' %}

{% block title %}New Emprunt{% endblock %}

{% block body %}
<div class="card">
    <div class="card-body">
        {{ include('emprunt/_form.html.twig') }}
        <a href="{{ path('emprunt_index') }}" class="btn btn-danger">  << retour en arrière </a>
    </div>
</div>
{% endblock %}
