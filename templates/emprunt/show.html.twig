{% extends 'base.html.twig' %}

{% block title %}Emprunt{% endblock %}

{% block body %}
    <h1>Emprunt</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ emprunt.id }}</td>
            </tr>
            <tr>
                <th>Numero_emprunt</th>
                <td>{{ emprunt.numeroEmprunt }}</td>
            </tr>
            <tr>
                <th>Valeur_emprunt</th>
                <td>{{ emprunt.valeurEmprunt }}</td>
            </tr>
            <tr>
                <th>Echeance</th>
                <td>{{ emprunt.echeance ? emprunt.echeance|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Taux_interet</th>
                <td>{{ emprunt.tauxInteret }}</td>
            </tr>
            <tr>
                <th>Interet_general</th>
                <td>{{ emprunt.interetGeneral }}</td>
            </tr>
            <tr>
                <th>Interet_mensuel</th>
                <td>{{ emprunt.interetMensuel }}</td>
            </tr>
            <tr>
                <th>Motif</th>
                <td>{{ emprunt.motif }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ emprunt.createdAt ? emprunt.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ emprunt.updatedAt ? emprunt.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('emprunt_index') }}">back to list</a>

    <a href="{{ path('emprunt_edit', {'id': emprunt.id}) }}">edit</a>

    {{ include('emprunt/_delete_form.html.twig') }}
{% endblock %}
