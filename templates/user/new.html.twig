{% extends 'base.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}

<div class="card">
    <div class="card-body">
    {{ include('user/_form.html.twig') }}

    
        <a href="{{ path('user_index') }}" class="btn btn-danger">  << retour en arrière </a>
    </div>
</div>

{% endblock %}
