{% extends 'base.html.twig' %}

{% block title %}New Remboursement{% endblock %}

{% block body %}
<div class="card">
    <div class="card-body">
        {{ include('remboursement/_form.html.twig') }}
    <a href="{{ path('remboursement_index') }}" class="btn btn-danger">  << retour en arrière </a>
    </div>
</div>

{% endblock %}
