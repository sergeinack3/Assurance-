<form method="post" action="{{ path('remboursement_delete', {'id': remboursement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ remboursement.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
