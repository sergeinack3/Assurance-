{% extends 'base.html.twig' %}

{% block title %}Remboursement{% endblock %}

{% block body %}
    <h1>Remboursement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ remboursement.id }}</td>
            </tr>
            <tr>
                <th>Motant</th>
                <td>{{ remboursement.motant }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ remboursement.createdAt ? remboursement.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ remboursement.updatedAt ? remboursement.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('remboursement_index') }}">back to list</a>

    <a href="{{ path('remboursement_edit', {'id': remboursement.id}) }}">edit</a>

    {{ include('remboursement/_delete_form.html.twig') }}
{% endblock %}
